export default function PaymentCard({ amount }) {
  return (
    <div>
      <p>Amount: {amount}</p>
      {/* <p>Expiry Date: {props.expiryDate}</p>
            <p>Card Holder Name: {props.cardHolderName}</p> */}
    </div>
  );
}
